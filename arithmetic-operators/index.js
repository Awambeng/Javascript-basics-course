// arithmetic operators = operands (values, variables, etc.)
//                          operators (+ - * /)
//                            example 11 = x + 5;

let student = 24;
/*
student = student * 2; // * represents multiplication
student = student + 1; // + represents addition
student = student - 1; // - represents subtraction
student = student / 2; // / represents division
student = student ** 2; // ** represents exponent
*/

let extrastudent = student % 5 // % represents modulo

console.log(`The number of students is: ${student}`);

console.log(`The remainder is: ${extrastudent}`);

